---
interface Props {
    title: string;
}

const { title } = Astro.props;
---
<article>
    <h2 class="border-b-2 border-solid border-black pb-1 uppercase font-bold tracking-[.2rem]">{title}</h2>
    <main class="pt-6 flex flex-col gap-y-5">
        <slot/>
    </main>
</article>