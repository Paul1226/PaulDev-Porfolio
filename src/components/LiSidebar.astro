---
interface Props {
    titulo: string
    url: string
}
const { titulo, url } = Astro.props
---

<li class="">
    <a href={url} class="flex flex-col justify-center items-center w-full [aspect-ratio:1/1] rounded-lg gap-1 p-1 hover:bg-chill-400/10 transition-colors duration-300 link">
        <slot name="icon" />
        <span class="leading-[10px] capitalize text-[10px]">{titulo}</span>
    </a>
</li>
<style>
    a.active{
        @apply bg-chill-700 pointer-events-none;

        svg{
            @apply fill-chill-50 stroke-chill-50;
        }
    }
</style>

<script>
    const $links = document.querySelectorAll<HTMLAnchorElement>('.link');
    const linkDefault = document.querySelector<HTMLAnchorElement>('.link')?.classList.add('active')
    $links.forEach(link=>{
        link.addEventListener('click', (e)=>{
            $links.forEach(link => link.classList.remove('active'))
            link.classList.add('active')
        })
    })
</script>